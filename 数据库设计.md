#   数据库设计

## 概述

系统采用统一用户模型，所有用户权限平等。用户可以创建活动，并邀请其他用户担任该活动的评委。

## ER 关系图

```
┌──────────┐       ┌──────────────┐       ┌──────────┐
│  users   │──1:N──│   events     │──1:N──│  teams   │
└──────────┘       └──────────────┘       └──────────┘
     │                    │                     │
     │               ┌────┴─────┐              │
     │               │          │              │
    N:M            event_     1:N              │
     │            judges   ┌───┴────┐          │
     │               │     │        │          │
     └───────────────┘  judges   scores        │
                       (users)     │           │
                          │        └───────────┘
                          └────────────────────┘
```

## 表结构

### users（用户表）

所有用户统一管理，无角色区分。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码（bcrypt 加密） |
| display_name | VARCHAR(50) | NOT NULL | 显示名称（评委牌上显示） |
| created_at | DATETIME | DEFAULT NOW() | 注册时间 |

### events（活动表）

一个用户创建一个活动，如"第三届知识科普大赛"。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 活动ID |
| name | VARCHAR(100) | NOT NULL | 活动名称 |
| creator_id | INT | FK → users.id | 创建者ID |
| status | VARCHAR(20) | DEFAULT 'pending' | 状态：pending / active / finished |
| created_at | DATETIME | DEFAULT NOW() | 创建时间 |

### event_judges（活动评委关联表）

记录每个活动的评委名单。创建者可以将自己和其他用户添加为评委。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| event_id | INT | FK → events.id | 活动ID |
| judge_id | INT | FK → users.id | 评委（用户）ID |
| created_at | DATETIME | DEFAULT NOW() | 添加时间 |

**唯一约束**：`UNIQUE(event_id, judge_id)` — 同一活动同一评委只能添加一次。

### teams（参赛队伍表）

每个活动下有若干参赛队伍。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 队伍ID |
| event_id | INT | FK → events.id | 所属活动ID |
| name | VARCHAR(100) | NOT NULL | 队伍名称 |
| status | VARCHAR(20) | DEFAULT 'waiting' | 状态：waiting / performing / finished |
| created_at | DATETIME | DEFAULT NOW() | 创建时间 |

**唯一约束**：`UNIQUE(event_id, name)` — 同一活动内队伍名不能重复。

### scores（评分表）

评委对参赛队伍的打分记录。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 主键 |
| event_id | INT | FK → events.id | 活动ID |
| team_id | INT | FK → teams.id | 队伍ID |
| judge_id | INT | FK → users.id | 评委ID |
| value | DECIMAL(5,2) | NOT NULL | 分数（支持小数，如 9.50） |
| created_at | DATETIME | DEFAULT NOW() | 打分时间 |

**唯一约束**：`UNIQUE(team_id, judge_id)` — 每个评委对同一队伍只能打一次分。

## 关系说明

| 关系 | 类型 | 说明 |
|------|------|------|
| users → events | 1:N | 一个用户可创建多个活动 |
| users ↔ events (via event_judges) | M:N | 一个用户可担任多个活动的评委，一个活动可有多个评委 |
| events → teams | 1:N | 一个活动有多个参赛队伍 |
| events → scores | 1:N | 一个活动有多条评分记录 |
| teams → scores | 1:N | 一个队伍有多条评分记录 |
| users → scores | 1:N | 一个评委有多条评分记录 |

## 典型查询场景

1. **获取某活动的评委列表**：通过 event_judges 关联查询
2. **获取某队伍的平均分**：对该队伍的所有 scores 取 AVG
3. **获取某活动的排名**：按队伍平均分降序排列
4. **判断评委是否已打分**：查询 scores 表中是否存在对应 team_id + judge_id 记录
